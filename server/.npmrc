# D25 test-infra: npm 11 default-deny allow-list (project scope only).
# Allow EXACTLY the mongodb-memory-server postinstall (downloads the OFFICIAL mongod binary
# from MongoDB Inc.'s own host, fastdl.mongodb.org). All other install scripts remain denied.
allow-scripts[]=mongodb-memory-server
allow-scripts[]=mongodb-memory-server-core
